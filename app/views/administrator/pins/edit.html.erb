<h1>Editing pin</h1>

<%= render 'form' %>

<%= link_to 'Show', [:administrator, @pin] %> |
<%= link_to 'Back', administrator_pins_path %>

<h1>Listing pins</h1>

<table>
  <tr>
    <th>Number</th>
    <th>Serial</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pins.each do |pin| %>
  <tr>
    <td><%= pin.number %></td>
    <td><%= pin.serial %></td>
    <td><%= link_to 'Show',[:administrator, pin] %></td>
    <td><%= link_to 'Edit', edit_administrator_pin_path(pin) %></td>
    <td><%= link_to 'Destroy', [:administrator, pin], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pin', new_administrator_pin_path %>

