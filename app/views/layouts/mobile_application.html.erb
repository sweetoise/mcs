<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title><%= content_for?(:title) ? yield(:title) : "MCS :: Home" %></title>
		<meta name="description" content="<%= content_for?(:description) ? yield(:description) : "App_Name" %>">
		<%= stylesheet_link_tag "mobile_application", :media => "all" %>
		<%= javascript_include_tag "mobile_application" %>
		<%= csrf_meta_tags %>
		<%= yield(:head) %>
		
	</head>
	<body>
		<div class="mobile_app_pagecontent">
			<header class="header_navbar">
				<table class="mobile_header_table">
					<tr>
						<td class="mobile_site_logo_table">
							<table>
								<tr>
									<td>
										<div class="mcs_tag">My Course Study</div></td><td><div class="separator_tag">|</div></td><td><div class="mobile_tag">Mobile</div>
									</td>
								</tr>
							</table>
							<table>
								<tr>
									<td>
										<div class="goal_tag">The&nbsp;&nbsp;Higher&nbsp;&nbsp;Institution&nbsp;&nbsp;Courses&nbsp;&nbsp;Archive</div>
									</td>
								</tr>
							</table>
						</td>
						<td class="mobile_site_signup">
						    <%= link_to 'SIGN UP',  new_user_registration_path, :method => 'delete', :class => 'register' %>
						</td>
					</tr>
				</table>
			</header>
			
			<!--! end of #main -->
		</div>
	</body>
</html>